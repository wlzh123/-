#include<iostream>
using namespace std;

int mul(int n,int m){
   if(m==0) return 1;
   if (m==1) return n;
   if(m%2==1) return mul(n,m/2)*mul(n,m/2)*n;
   return mul(n,m/2)*mul(n,m/2);

}
int main(){

    int x=5;
    int y=2;
    int res=mul(2,8);
    cout<<res<<endl;
}
