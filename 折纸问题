#include<iostream>
using namespace std;
void Folder(int i,int n,bool down)
{
    if(i>n) return;
    Folder(i+1,n,true);
    cout<<(down?"down":"up")<<endl;
    Folder(i+1,n,false);
}
int main()
{
    int N=2;//折N次
    Folder(1,N,true);
    return 0;
}
